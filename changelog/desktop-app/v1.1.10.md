# desktop-app v1.1.10

- Fixed packaged sidecar dependency resolution by unpacking runtime `node_modules` for bundled Node processes.
- This addresses `narre-server` startup crashes before `narre-server.log` initialization in packaged builds.
