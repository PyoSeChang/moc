# desktop-app v1.1.11

- Fixed packaged Narre sidecar startup by shipping `zod`, `@modelcontextprotocol/sdk`, and related OpenAI runtime dependencies with the desktop app.
- Keeps the unpacked runtime `node_modules` layout used by bundled Node sidecars.
