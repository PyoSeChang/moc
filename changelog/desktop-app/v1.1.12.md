# desktop-app v1.1.12

- Packaged Narre now launches from a dedicated copied sidecar artifact instead of relying on the packaged workspace dependency layout.
- This avoids stale `@netior/narre-server` package contents in installed builds and fixes pre-log startup crashes.
