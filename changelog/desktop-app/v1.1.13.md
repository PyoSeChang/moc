# desktop-app v1.1.13

- Packaged Narre now uses a dedicated copied sidecar artifact plus its runtime `node_modules`, avoiding stale packaged workspace output.
- `narre-server` OpenAI/MCP dependencies stay bundled while `express`, `cors`, and Claude SDK runtime packages are copied alongside the sidecar.
