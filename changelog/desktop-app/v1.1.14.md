# desktop-app v1.1.14

- Packaged Narre now prefers the packaged `node_modules/@netior/narre-server` path, which preserves the full runtime dependency tree.
- `@netior/narre-server` now builds as normal ESM output instead of a single bundled file, avoiding Express/body-parser dynamic-require failures in the packaged sidecar.
