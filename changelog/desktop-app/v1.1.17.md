# desktop-app v1.1.17

- Fixed packaged `netior-service` startup by restoring the `better_sqlite3.node` native binding in the sidecar payload.
- Fixed packaged `narre-server` startup by shipping it as a bundled sidecar instead of a deeply nested dependency tree.
- This release targets installer/runtime stability for the packaged Windows app.
