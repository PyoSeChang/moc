# desktop-app v1.1.18

- Fixed packaged `narre-server` startup by shipping it as a bundled CommonJS sidecar instead of a nested dependency tree.
- Kept the packaged `netior-service` native SQLite binding in the sidecar payload.
- This release targets packaged Windows app startup reliability for both data and Narre services.
