# desktop-app v1.1.19

- Added `netior-mcp` to the packaged sidecar payload so Narre can launch MCP tools in the installed app.
- Keeps the bundled CJS `narre-server` sidecar and packaged `netior-service` native SQLite binding.
- This release targets packaged Narre startup parity with the workspace build.
