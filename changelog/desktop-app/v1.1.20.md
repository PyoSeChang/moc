# desktop-app v1.1.20

- Bundled `netior-mcp` as a packaged sidecar instead of shipping a deployed dependency tree.
- Updated packaged Narre to resolve the bundled MCP sidecar first.
- This reduces installer/uninstaller fragility from deeply nested sidecar dependency trees.
