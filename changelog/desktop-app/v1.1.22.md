# desktop-app v1.1.22

- Added an NSIS installer recovery hook for upgrades blocked by a failed old-version uninstaller.
- If the old app is not actually running, the installer now attempts a manual cleanup of the old install directory and uninstall registry entry.
- Keeps the bundled sidecar packaging changes from recent desktop releases.
