# desktop-app v1.1.23

- Added an installer-side pre-cleanup path for previous Netior installs when no running `Netior.exe` process is detected.
- This avoids depending on broken old uninstallers during upgrades and prevents the false `Netior cannot be closed` flow from blocking installation.
- Keeps the `1.1.22` manual cleanup fallback as a secondary recovery path.
