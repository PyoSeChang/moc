---
package: "desktop-app"
version: "1.1.8"
date: 2026-04-11
type: patch
summary: "Fixed packaged Narre startup and improved main window restore behavior"
---

## Fixes
- Lazy-loaded OpenAI/Codex provider adapters in `narre-server` so the Claude path no longer fails during module import in the packaged app.
- Persisted and restored main window state with display-aware bounds handling to avoid reopening on stale monitor coordinates.
- Added main-process file logging and safer timeout fallbacks for packaged startup diagnostics.

## Internal
- Kept packaged Node sidecar path resolution aligned with `app.asar.unpacked`.
