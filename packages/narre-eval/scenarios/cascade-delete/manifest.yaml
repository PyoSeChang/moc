id: cascade-delete
title: "종속 데이터가 있는 아크타입 삭제 확인"
description: "종속 데이터가 있는 아크타입 삭제 시 경고 확인"
scenario_version: "1.0.0"
schema_version: 1
type: conversation
lifecycle: active
labels: [archetype, delete, cascade]

execution:
  supported_agents: [narre-server]
  required_capabilities: [tool_call_trace]

turn_plan:
  file: turns.yaml

entrypoints:
  seed: seed.ts

assets:
  verify:
    - verify/checks.yaml
  rubrics:
    - rubrics/quality.yaml

provenance:
  created_by:
    id: netior-team
    name: Netior Team
    source: repo-scenario
