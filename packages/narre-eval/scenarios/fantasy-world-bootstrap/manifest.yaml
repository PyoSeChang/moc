id: fantasy-world-bootstrap
title: "판타지 소설 부트스트랩"
description: "도메인 설명만으로 /bootstrap skill이 ontology를 먼저 읽고, 두 번의 짧은 interview로 entity/artifact/workflow/관계를 확인한 뒤, 그 ontology를 네트워크 구조, archetype, traits, ORM-style field, starter graph로 투영하는지 평가한다"
scenario_version: "3.1.0"
schema_version: 1
type: single-turn
lifecycle: active
labels: [product, fantasy, worldbuilding, ontology, network, traits, orm, bootstrap, skill]
responsibility_surfaces: [NR02, NR05, NR15, NR16, NR17, NR18, NR19, NR24]

execution:
  supported_agents: [narre-server]
  required_capabilities: [tool_call_trace]
  target_skill: bootstrap
  scenario_kind: interpretive
  agent_id: narre-basic
  provider: codex
  tester: codex-tester
  analysis_targets: [tool_use, schema, networks, interview]

turn_plan:
  file: turns.yaml

entrypoints:
  seed: seed.ts

assets:
  verify:
    - verify/checks.yaml
  rubrics:
    - rubrics/quality.yaml

provenance:
  created_by:
    id: netior-team
    name: Netior Team
    source: repo-scenario
