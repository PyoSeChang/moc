id: type-update
title: "기존 아크타입 이름 변경"
description: "기존 아크타입 이름 변경"
scenario_version: "1.0.0"
schema_version: 1
type: single-turn
lifecycle: active
labels: [archetype, update]

execution:
  supported_agents: [narre-server]
  required_capabilities: [tool_call_trace]

turn_plan:
  file: turns.yaml

entrypoints:
  seed: seed.ts

assets:
  verify:
    - verify/checks.yaml
  rubrics:
    - rubrics/quality.yaml

provenance:
  created_by:
    id: netior-team
    name: Netior Team
    source: repo-scenario
